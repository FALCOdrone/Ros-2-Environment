To start gazebo11 run:

```
    ros2 launch gazebo_ros gazebo.launch.py
```

Then in a new terminal navigate to the path until **urdf** folder and then run the following bash script that spawns the drone inside gazebo11 simulation environment:

```
    ./spawnrobot.sh
```
